import * as React from "react";

import VideoContainer from './containers/VideoContainer';


export const App = () => (
    <div className="container-fluid" >
        <VideoContainer />
        <footer className="text-center">Desenvolvido por Wéder Pacheco</footer>
    </div>
);